> ⚠️ AI-Generated Module / Generated by Bob, an AI assistant powered by IBM watsonx

# eks-vault-vcs

VCS-driven HCP Terraform workspace that deploys **HashiCorp Vault** (community or enterprise)
on the EKS cluster created by `eks-infra-vcs`.

## Architecture

```
eks-infra-vcs  ──run trigger──▶  eks-vault-vcs
      │                                │
      │  tfe_outputs (cluster_name,    │  acme provider → Let's Encrypt cert
      │  cluster_endpoint, cluster_ca, │  DNS-01 via Route 53 (jose-merchan.sbx.hashidemos.io)
      │  aws_region)                   │  kubernetes secret (vault-tls)
      └────────────────────────────────▶  helm_release vault → NLB (internet-facing)
                                          aws_route53_record → vault.jose-merchan.sbx.hashidemos.io
```

## Vault editions

| `vault_edition` | Image | Licence secret required |
|---|---|---|
| `community` (default) | `hashicorp/vault` | No |
| `enterprise` | `hashicorp/vault-enterprise` | Yes — set `vault_enterprise_license` (sensitive) |

## Key workspace variables

| Variable | Default | Notes |
|---|---|---|
| `vault_edition` | `community` | Set to `enterprise` for ENT |
| `vault_hostname` | `vault` | Combined with `base_domain` |
| `base_domain` | `jose-merchan.sbx.hashidemos.io` | Route 53 zone |
| `acme_email` | `jose.merchan@hashicorp.com` | Let's Encrypt contact |
| `vault_replicas` | `3` | HA Raft cluster size |
| `vault_enterprise_license` | `""` | Sensitive — set in HCP Terraform UI |

## Outputs

| Output | Description |
|---|---|
| `vault_url` | `https://vault.jose-merchan.sbx.hashidemos.io:8200` |
| `vault_ui_url` | `https://vault.jose-merchan.sbx.hashidemos.io:8200/ui` |
| `vault_load_balancer_hostname` | AWS NLB hostname |
| `certificate_expiry` | Let's Encrypt cert expiry |
